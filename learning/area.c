#include <stdio.h>

int main(){
    int l,b;

    printf("Enter the length-> ");
    scanf("%d", &l);

    printf("Enter the width-> ");
    scanf("%d", &b);

    printf("the area of rectangle is %d", l*b);
    return 0;
}