#include <stdio.h>


int main()
{
    int a = 1;
    int y = 2;
    int z = 46;
    int x = 3;
    int m = 3.0 / 9;
    float b = 2.3;
    char c = 'm';
    printf("The value of an intiger is %d %d %d \n", a, y, x);
    printf("The value of an non-intiger is %f \n", b);
    printf("3.0/9 %d \n", 3.0/9);
    printf("The value of an character is %c \n", c);
    printf("The sum of z and y is %d, \n", z + y);
    printf("The substraction of z and y is %d, \n", z - y);
    printf("The multiply of z and y is %d, \n", z * y);
    printf("The division of z and y is %d, \n", z / y);

    return 0;
}